---
phase: 02-content-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/AppHeader.vue
  - app/components/AppFooter.vue
autonomous: true

must_haves:
  truths:
    - "Header uses a gradient fade divider instead of a hard border at its bottom edge"
    - "Footer uses a gradient fade divider instead of a hard border at its top edge"
    - "Gradient dividers fade from transparent to gold/20 to transparent horizontally"
  artifacts:
    - path: "app/components/AppHeader.vue"
      provides: "Header with gradient fade divider replacing border-b"
      contains: "bg-gradient-to-r"
    - path: "app/components/AppFooter.vue"
      provides: "Footer with gradient fade divider replacing border-t"
      contains: "bg-gradient-to-r"
  key_links:
    - from: "app/components/AppHeader.vue"
      to: "gradient divider"
      via: "h-px div with gradient background after nav content"
      pattern: "h-px.*bg-gradient-to-r.*via-gold"
    - from: "app/components/AppFooter.vue"
      to: "gradient divider"
      via: "h-px div with gradient background before footer content"
      pattern: "h-px.*bg-gradient-to-r.*via-gold"
---

<objective>
Replace hard border lines on the header and footer with gradient fade dividers that transition from transparent to subtle gold to transparent, creating premium section separation.

Purpose: Hard 1px borders look flat and utilitarian. Gradient fade dividers create a softer, more refined visual separation between header/content/footer that elevates the overall premium feel.
Output: Updated AppHeader.vue and AppFooter.vue with gradient divider elements replacing border-b and border-t classes.
</objective>

<execution_context>
@/home/aph5nt/.claude/get-shit-done/workflows/execute-plan.md
@/home/aph5nt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-polish/02-RESEARCH.md
@app/components/AppHeader.vue
@app/components/AppFooter.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace header and footer hard borders with gradient fade dividers</name>
  <files>app/components/AppHeader.vue, app/components/AppFooter.vue</files>
  <action>
**AppHeader.vue changes (DIVD-01, DIVD-02):**
1. Remove `border-b border-dark-border` from the `<header>` element's class attribute
2. Keep all other classes: `sticky top-0 z-50 bg-dark/80 backdrop-blur-md`
3. Add a gradient divider `<div>` as the LAST child inside `<header>`, after the nav wrapper div:
   ```html
   <div class="h-px bg-gradient-to-r from-transparent via-gold/20 to-transparent"></div>
   ```
4. The gradient div must be INSIDE the `<header>` tag (so it inherits sticky positioning and backdrop-blur)

**AppFooter.vue changes (DIVD-01, DIVD-02):**
1. Remove `border-t border-dark-border` from the `<footer>` element's class attribute
2. Keep `mt-auto` on `<footer>`, remove `py-8` from footer (padding moves to content div below)
3. Add a gradient divider `<div>` as the FIRST child inside `<footer>`, before the content wrapper div:
   ```html
   <div class="h-px bg-gradient-to-r from-transparent via-gold/20 to-transparent"></div>
   ```
4. Ensure the content wrapper div below the gradient has `py-8` padding so spacing is preserved (CRITICAL: Pitfall 6 from research — the gradient div is outside the padding container, so pt-8 must be on the content div)

The `<script>` block in AppHeader.vue stays unchanged. AppFooter.vue has no script block.

Do NOT change nav content, links, logo, or any other element — only the border treatment.
  </action>
  <verify>
Run: `npx nuxi build` — build must succeed with no errors.
Check AppHeader.vue:
- No `border-b` or `border-dark-border` in header class
- Contains `h-px bg-gradient-to-r from-transparent via-gold/20 to-transparent` div inside header
Check AppFooter.vue:
- No `border-t` or `border-dark-border` in footer class
- Contains `h-px bg-gradient-to-r from-transparent via-gold/20 to-transparent` div inside footer
- Content wrapper div has py-8 padding
  </verify>
  <done>
Header bottom edge shows a horizontal gradient that fades from transparent through subtle gold (20% opacity) to transparent, replacing the hard dark border. Footer top edge shows the same gradient fade. Both dividers are 1px height. Footer content spacing is preserved with py-8 on the content wrapper.
  </done>
</task>

</tasks>

<verification>
After task completion, run `npx nuxi build` and confirm:
1. Build succeeds (29 routes prerendered)
2. No `border-b border-dark-border` in AppHeader.vue
3. No `border-t border-dark-border` in AppFooter.vue
4. Both files contain gradient divider div elements
5. Footer spacing is preserved (py-8 on content wrapper)
</verification>

<success_criteria>
- Header: gradient fade divider replaces hard border, sticky behavior and backdrop-blur preserved
- Footer: gradient fade divider replaces hard border, vertical spacing preserved
- Both dividers: 1px height, horizontal gradient from transparent via gold/20 to transparent
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-polish/02-02-SUMMARY.md`
</output>
